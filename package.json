{
  "name": "paperdoctor-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"cd api && uvicorn main:app --reload --port 8000\" \"cd frontend && npm run dev\"",
    "dev:api": "cd api && uvicorn main:app --reload --port 8000",
    "dev:web": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
